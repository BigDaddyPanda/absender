@model IEnumerable<AbsenderAPI.Models.UniversityModels.Module>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Designation_Module)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ref_Filiere)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Designation_Module)
            </td>
            <td> 
                @Html.DisplayFor(modelItem => item.Ref_Filiere.Designation_Niveau)
                @Html.DisplayFor(modelItem => item.Ref_Filiere.Designation_Filiere)
                @Html.DisplayFor(modelItem => item.Ref_Filiere.Designation_Option)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Module">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Module">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Module">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
