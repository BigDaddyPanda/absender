@model IEnumerable<AbsenderAPI.Models.UniversityModels.Filiere>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Designation_Niveau)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Designation_Filiere)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Designation_Option)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ref_Diplome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Designation_Niveau)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Designation_Filiere)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Designation_Option)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ref_Diplome.Designation_Formation)/  @Html.DisplayFor(modelItem => item.Ref_Diplome.Designation_Orientation)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Filiere">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Filiere">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Filiere">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
