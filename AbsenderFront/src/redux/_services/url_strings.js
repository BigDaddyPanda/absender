export const API_URL = "http://localhost:5000/";
// export const API_URL="/";

/**
 * to reduce confusion between right routes, 
 * it is better to have the same file shared between front and back
 * and then simply call variables indication a string for the route 
 * than stripping your hair over a miss spell of that route name
 */
// example
// export const PING_URL = "/ping";